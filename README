# Async Log Analyzer

Учебный проект (Endterm): асинхронная система мониторинга и анализа логов.

## Цель

Показать использование:
- асинхронного программирования (`asyncio`);
- генераторов и итераторов;
- декораторов (логирование и измерение времени);
- многопоточности (`ThreadPoolExecutor`);
- пользовательских исключений;
- модульной архитектуры проекта.

## Формат лог-файла

Каждая строка должна иметь вид:

```text
[YYYY-MM-DD HH:MM:SS] [LEVEL] Message text...
